import React from 'react';
import { TResource } from '../types';

interface Props {
  resource: TResource;
}

const Shelters = (props: Props) => (
  <div>
    <p>Shelters</p>
  </div>
);

export default Shelters;
